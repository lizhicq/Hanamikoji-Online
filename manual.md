pkill -f "node hanamikoji_full.js" && node hanamikoji_full.js


运行状态
游戏服务器正在运行中：

本地地址: http://localhost:3000
在线隧道: 您可以使用 npx localtunnel --port 3000 再次生成在线网址（之前的进程可能已结束，建议重新运行以获取新链接）。
如何使用
查看手册: 请阅读 README_MANUAL.md 获取详细步骤。
开始游戏: 在浏览器打开 http://localhost:3000 即可体验修复后的游戏。
在线对战: 运行 npx localtunnel --port 3000，将生成的 URL 发送给朋友，一人创建游戏，另一人输入 ID 加入。